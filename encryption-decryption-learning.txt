Repository ini berisi contoh dan penjelasan dasar mengenai enkripsi dan dekripsi, ditujukan untuk pembelajaran keamanan siber dan kriptografi. Semua contoh aman untuk dijalankan secara lokal dan tidak digunakan untuk aktivitas ilegal.

1️⃣ Apa itu Enkripsi?

Enkripsi adalah proses mengubah data asli (plaintext) menjadi bentuk yang tidak bisa dibaca (ciphertext) menggunakan algoritma tertentu dan key. Tujuannya adalah melindungi data agar tidak dibaca pihak yang tidak berwenang.
Contoh sederhana:
Plaintext: HELLO
Key: 3
Ciphertext (Caesar Cipher): KHOOR

2️⃣ Apa itu Dekripsi?

Dekripsi adalah proses mengembalikan ciphertext menjadi data asli menggunakan key yang sama (untuk symmetric encryption) atau key berbeda (untuk asymmetric encryption).

Contoh:
Ciphertext: KHOOR
Key: 3
Plaintext asli: HELLO

3️⃣ Jenis Enkripsi yang Bisa Dipelajari

Symmetric Encryption: menggunakan key yang sama untuk enkripsi dan dekripsi. Contoh: AES, DES.

Asymmetric Encryption: menggunakan key berbeda untuk enkripsi dan dekripsi. Contoh: RSA, ECC.

Hashing: membuat fingerprint data. Contoh: SHA256, MD5.

4️⃣ Contoh Implementasi di Python

# Contoh sederhana Caesar Cipher
def encrypt(text, key):
    return ''.join(chr((ord(c) - 65 + key) % 26 + 65) if c.isupper() else c for c in text)

def decrypt(cipher, key):
    return ''.join(chr((ord(c) - 65 - key) % 26 + 65) if c.isupper() else c for c in cipher)

plaintext = "HELLO"
key = 3

ciphertext = encrypt(plaintext, key)
print("Ciphertext:", ciphertext)

decrypted = decrypt(ciphertext, key)
print("Decrypted:", decrypted)

Membantu pemula memahami konsep enkripsi & dekripsi.

Memberikan contoh sederhana untuk eksperimen di lingkungan lokal.

Dasar pembelajaran untuk keamanan siber atau pengembangan tools yang aman.

Semua contoh hanya untuk pembelajaran.

Jangan gunakan untuk membuka ransomware nyata atau menyerang sistem orang lain.
